<ol class="post-list">
    <li>
        <article class="post">
            <div class="name">
                <h2>{{post.name}}</h2>
                <h3>{{post.location}}</h3>
                <p>Posted by: {{User.username}}</p>
            </div>
            <div class="meta">
                <a href="/post/{{id}}">{{comments.length}} comment(s)</a>
            </div>
        </article>
    </li>
</ol>