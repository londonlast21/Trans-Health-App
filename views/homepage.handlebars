<div >
                <img src="transflag.png" alt="transgender pride flag" width="300" />
            </div>

<ul style="list-style-type:none; text-align: center; class="pure-u-1">
    <p>{{posts.length}} provider(s in the system</p>
    {{#each posts}}
  <li>
      
    <article class="pure-u-1" style="text-align:center;">
        
      
       <!--part for the info from post-->
        <h2>{{name}}</h2>
        <h4>{{location}}</h4>
        <h4>{{specialty}}</h4>
      

    <!--comments div-->
      <div class="pure-u-1">
        <a href="/post/{{id}}">{{comments.length}} comment(s)</a>
      </div>
    </article>
  </li>
  {{/each}}
</ul>